spring:
  flyway:
    locations: db/migrations
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/electronic_shop_tvo}
    password: ${DB_PASSWORD:pokus}
    username: root
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${AZURE_ISSUER_URI}
          audiences:
            - ${AZURE_CLIENT_ID}
            - api://${AZURE_CLIENT_ID}
server:
  port: 1409
  servlet:
    context-path: "/api"